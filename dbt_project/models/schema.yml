version: 2

sources:
  - name: raw
    schema: RAW
    tables:
      - name: users
        identifier: raw_users
        columns:
          - name: id
            tests:
              - not_null
              - unique
          - name: email
            tests:
              - not_null

models:
  - name: customers
    columns:
      - name: id
        tests:
          - not_null
          - unique
      - name: email
        tests:
          - not_null
